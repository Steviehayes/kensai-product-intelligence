services:
  agent:
    build: .
    ports:
      - "8000:7860"        # open http://localhost:8000
    environment:
      - PORT=7860
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}   # optional: enables Gemini chat + embeddings
      - GEMINI_CHAT_MODEL=${GEMINI_CHAT_MODEL:-gemini-2.0-flash}
